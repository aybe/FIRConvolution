TODO

- copyto for scalar with stride/offset
- make net allocator aligned

add defines for attributes

use aligned allocation

var pointer = BurstCompiler.CompileFunctionPointer<FilterMethod>(Filters.VectorFullBandInner);

var method = pointer.Invoke;

var filter = default(Filter);

method(null, null, 0, ref filter);

using Unity.Mathematics;
#if DEBUG_HALF_BAND_START_TAP
using System.Diagnostics;
#endif




----

optimize
perf markers

rename filters
